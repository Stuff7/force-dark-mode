{
  "name": "dark-mode",
  "version": "2.0.0",
  "description": "Invert site colors.",
  "main": "src/script.js",
  "author": "Armando",
  "license": "MIT",
  "scripts": {
    "build": "web-ext build --artifacts-dir build --overwrite-dest --filename dark-mode.xpi"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "web-ext": "^8.8.0"
  }
}
